const fs=require('fs');
(async()=>{const m=await require(require('path').resolve('tmp/mpegh-test/encoder.cjs'))({wasmBinary:fs.readFileSync('tmp/mpegh-test/encoder.wasm')});
const count=144000;for(let o=0;o<2;o++){const w=Buffer.alloc(44+count*2);w.write('RIFF');w.writeUInt32LE(w.length-8,4);w.write('WAVEfmt ',8);w.writeUInt32LE(16,16);w.writeUInt16LE(1,20);w.writeUInt16LE(1,22);w.writeUInt32LE(48000,24);w.writeUInt32LE(96000,28);w.writeUInt16LE(2,32);w.writeUInt16LE(16,34);w.write('data',36);w.writeUInt32LE(count*2,40);for(let s=0;s<count;s++)w.writeInt16LE(Math.round(2000*Math.sin(s*2*Math.PI*(440+o*220)/48000)),44+s*2);m.FS.writeFile('/motion_00'+o+'.wav',w);}
const frames=145,head=170,b=Buffer.alloc(head+frames*52);b.write('OAM ');b.writeUInt16LE(1,4);b.writeUInt16LE(2,8);for(let f=0;f<frames;f++)for(let o=0;o<2;o++){let p=head+(f*2+o)*26;b.writeBigUInt64LE(BigInt((f+1)*1024),p);b.writeUInt16LE(o,p+8);b.writeFloatLE((o?1:-1)*(-80+160*f/(frames-1)),p+10);b.writeFloatLE(o?30:0,p+14);b.writeFloatLE(1,p+18);b.writeFloatLE(1,p+22);}m.FS.writeFile('/motion.oam',b);m.callMain(['-oam_file:/motion.oam','-ofile:/motion.mhas','-op_fmt:1','-br:192000','-cicp:6']);fs.writeFileSync('tmp/mpegh-test/motion.mhas',m.FS.readFile('/motion.mhas'));})();
